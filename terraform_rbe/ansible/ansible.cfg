[defaults]
host_key_checking = False
timeout = 60

[ssh_connection]
# Give sudo more room to respond under load (e.g. concurrent apt/docker
# activity on source_vm_id) than the 12s Ansible default.
ssh_args = -o ControlMaster=auto -o ControlPersist=60s

[privilege_escalation]
timeout = 60